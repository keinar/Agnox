# ===================================================================
# .env.example — Agnostic Automation Center
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.
# ===================================================================

# ===================================================================
# 🛡️  PLATFORM INFRASTRUCTURE SECRETS  (SECURITY_PLAN §1.3)
# Names prefixed with PLATFORM_ are internal to the AAC platform.
# These MUST NOT be injected into user test containers — the worker
# enforces this via PLATFORM_SECRET_BLOCKLIST.
# Generate secrets with: openssl rand -hex 32
# ===================================================================
PLATFORM_MONGO_URI=<REDACTED_MONGO_URI>
PLATFORM_REDIS_URL=<REDACTED_REDIS_URL>
PLATFORM_RABBITMQ_URL=<REDACTED_RABBITMQ_URL>
PLATFORM_GEMINI_API_KEY=<REDACTED_GOOGLE_API_KEY>
PLATFORM_JWT_SECRET=<REDACTED_JWT_SECRET>
PLATFORM_WORKER_CALLBACK_SECRET=<REDACTED_WORKER_SECRET>
# Set to 'true' during the 24h deploy transition window; set 'false' or remove after
WORKER_CALLBACK_TRANSITION=false

# ===================================================================
# 🔒  ENCRYPTION
# ===================================================================
ENCRYPTION_KEY=<64-char-hex-key>

# ===================================================================
# 🧪  TEST ENVIRONMENT INJECTION  (The "App Under Test")
# These are SAFE variables passed into user test containers.
# ⚠️  Do NOT add any PLATFORM_* names to INJECT_ENV_VARS — they are
# blocked by the PLATFORM_SECRET_BLOCKLIST in the worker.
# ===================================================================
INJECT_ENV_VARS=ADMIN_USER,ADMIN_PASS
ADMIN_USER=<REDACTED_ADMIN_USER>
ADMIN_PASS=<REDACTED_ADMIN_PASS>
# MONGO_URI below refers to the App-Under-Test database (NOT the platform)
# MONGO_URI=<REDACTED_MONGO_URI>

BASE_URL=https://your-app.example.com

# ===================================================================
# 🏗️  APP CONFIGURATION
# ===================================================================
NODE_ENV=production
PORT=3000
REPORTS_DIR=/app/reports
JWT_EXPIRY=24h
PASSWORD_SALT_ROUNDS=10

# URLs
VITE_API_URL=https://api.your-domain.com
PUBLIC_API_URL=https://api.your-domain.com
DASHBOARD_URL=https://dashboard.your-domain.com
FRONTEND_URL=https://your-domain.com

# CORS (comma-separated allowed origins)
ALLOWED_ORIGINS=https://dashboard.your-domain.com

# Rate Limiting
LOG_RATE_LIMIT=false

# ===================================================================
# 🧪  DEFAULT TEST SETTINGS
# ===================================================================
DEFAULT_TEST_IMAGE=your-org/your-test-image:latest
DEFAULT_BASE_URL=https://your-app.example.com
DEFAULT_TEST_FOLDER=tests
DEV_URL=http://localhost:5173
STAGING_URL=https://staging.your-app.example.com
PRODUCTION_URL=https://your-app.example.com

# ===================================================================
# 📦  INTEGRATIONS
# ===================================================================

# Stripe
STRIPE_SECRET_KEY=<REDACTED_STRIPE_SECRET_KEY>
STRIPE_PUBLISHABLE_KEY=<REDACTED_STRIPE_PUBLISHABLE_KEY>
STRIPE_WEBHOOK_SECRET=<REDACTED_STRIPE_WEBHOOK_SECRET>
STRIPE_TEAM_PRICE_ID=<REDACTED_STRIPE_PRICE_ID>
STRIPE_ENTERPRISE_PRICE_ID=<REDACTED_STRIPE_PRICE_ID>

# SendGrid
SENDGRID_API_KEY=<REDACTED_SENDGRID_API_KEY>
FROM_EMAIL=noreply@your-domain.com
FROM_NAME=Agnostic Automation Center
EMAIL_RETRY_MAX_ATTEMPTS=3
EMAIL_RETRY_INITIAL_DELAY=1000
EMAIL_TRACKING_ENABLED=true
EMAIL_OPEN_TRACKING=true
EMAIL_CLICK_TRACKING=true

# GitHub Copilot (optional)
GITHUB_COPILOT_API_KEY=<REDACTED_GITHUB_API_KEY>
